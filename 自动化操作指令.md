# 自动化操作指令文档

本文档记录项目开发过程中使用的自动化命令，便于后续快速执行相同操作。

## PowerShell 运行命令

### 编译项目
```powershell
& "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\MSBuild\Current\Bin\MSBuild.exe" CodeGeneratorOpenness\CodeGeneratorOpenness.csproj /p:Configuration=Debug /p:Platform=AnyCPU 
```

### 运行程序
```powershell
.\CodeGeneratorOpenness\bin\Debug\CodeGeneratorOpenness.exe
```

### 强制终止进程
```powershell
taskkill /F /IM CodeGeneratorOpenness.exe
```

## 常用操作流程

### 代码修改后的验证流程
1. 编译项目：`& "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\MSBuild\Current\Bin\MSBuild.exe" CodeGeneratorOpenness\CodeGeneratorOpenness.csproj /p:Configuration=Debug /p:Platform=AnyCPU`
2. 运行程序：`.\CodeGeneratorOpenness\bin\Debug\CodeGeneratorOpenness.exe`
3. 如遇进程锁定：`taskkill /F /IM CodeGeneratorOpenness.exe`
4. 重新编译：`& "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\MSBuild\Current\Bin\MSBuild.exe" CodeGeneratorOpenness\CodeGeneratorOpenness.csproj /p:Configuration=Debug /p:Platform=AnyCPU`

## 注意事项
- 编译前确保没有程序正在运行，避免文件锁定
- 使用相对路径运行程序，确保在项目根目录执行
- 强制终止进程仅在必要时使用

---
*最后更新：2025-01-27*