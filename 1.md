## 实际业务代码中使用原始TIA Portal Openness API的文件清单
根据代码搜索分析，以下是需要逐步迁移到 TiaPortalOpennessManager 封装类的业务代码文件：

### 1. 主要业务文件 `frmStartup.cs`
- 直接API调用 ： new Siemens.Engineering.TiaPortal() 、 tiaPortal.Projects.Open()
- 功能 ：启动窗体，负责TIA Portal连接和项目打开
- 迁移优先级 ：高（核心启动逻辑） `frmMainForm.cs`
- 直接API调用 ：
  - TiaPortal 实例管理
  - Project 、 Device 、 DeviceItem 、 PlcSoftware 操作
  - PlcBlock 、 PlcBlockGroup 处理
  - TiaPortal.GetProcesses() 、 TiaPortal.GetProcess()
- 功能 ：主窗体，包含设备遍历、项目树构建、块操作等核心功能
- 迁移优先级 ：高（主要业务逻辑） `cFunctionGroups.cs`
- 直接API调用 ：
  - PlcSoftware 结构加载
  - PlcBlock 、 PlcBlockGroup 遍历和操作
  - 各种块类型（OB、FB、FC、DB）处理
- 功能 ：功能组管理，负责项目树结构的自动加载
- 迁移优先级 ：中（辅助功能） `cContextMenu.cs`
- 直接API调用 ：
  - PlcBlock 、 PlcBlockGroup 判断
  - PlcUnit.CreateFrom() 操作
  - ProjectLibrary 相关操作
- 功能 ：右键菜单功能
- 迁移优先级 ：中（UI交互功能）
### 2. 辅助文件 `cJsonExporter.cs`
- 直接API调用 ： PlcSoftware 类型判断和处理
- 功能 ：JSON导出功能
- 迁移优先级 ：低（导出工具） `cTranlate.cs`
- 直接API调用 ：仅文本翻译，影响较小
- 迁移优先级 ：低（界面翻译）
### 3. 迁移建议
第一阶段 （高优先级）：

1. 1.
   frmStartup.cs - 迁移TIA Portal连接和项目打开逻辑
2. 2.
   frmMainForm.cs - 迁移核心设备遍历和项目操作逻辑
第二阶段 （中优先级）：
3. cFunctionGroups.cs - 迁移项目树结构加载逻辑
4. cContextMenu.cs - 迁移右键菜单操作逻辑

第三阶段 （低优先级）：
5. cJsonExporter.cs - 迁移导出功能
6. cTranlate.cs - 保持现状或最后处理

### 4. 迁移收益
- 代码统一性 ：所有TIA Portal操作通过统一接口
- 错误处理 ：集中的异常处理和日志记录
- 功能扩展 ：新增的命名空间批量修改等功能
- 维护性 ：降低代码耦合度，提高可维护性